# Prediction artifacts

The evaluation pipeline writes `validation_predictions.csv` under each experiment directory after a completed run. The source archive used to reconstruct this repository did not include the completed binary prediction CSVs from the Colab runtime, so no synthetic prediction rows are provided here.

For the reviewer response, the exact prediction files from the completed run should be copied into this directory before public release.
