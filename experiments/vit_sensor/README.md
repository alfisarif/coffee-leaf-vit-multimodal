# Experiment

Use the experiment name corresponding to this directory with `src.train` and the YAML files under `configs/`.

The reported final checkpoint epoch and metrics are documented in `results/reported_final_results.csv`. Binary checkpoint bytes and exact validation prediction files from the completed Colab runtime are not present in the source archive used to assemble this repository. They must be copied from the completed runtime before claiming binary-level reproducibility.
